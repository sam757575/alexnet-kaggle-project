[tool.poetry]
name = "alexnet-kaggle-project"
version = "0.1.0"
description = "A project implementing the AlexNet deep learning architecture using a Kaggle dataset."
authors = ["Your Name <youremail@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
tensorflow = "^2.6.0"
keras = "^2.6.0"
numpy = "^1.21.0"
pandas = "^1.3.0"
matplotlib = "^3.4.0"
seaborn = "^0.11.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
